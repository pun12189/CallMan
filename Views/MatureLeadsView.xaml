<UserControl x:Class="BahiKitab.Views.MatureLeadsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BahiKitab.Views"
             xmlns:core="clr-namespace:BahiKitab.Core"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:helper="clr-namespace:BahiKitab.Helper"
             mc:Ignorable="d">
    <UserControl.Resources>
        <core:IdToButtonTextConverter x:Key="IdToButtonTextConverter"/>
        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#20b2aa" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="10">

        <!-- Left Column: Leads List (DataGrid) -->
        <StackPanel Margin="0,0,10,0">
            <TextBlock Text="Customers" Style="{StaticResource TitleStyle}" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

            <!-- Command Buttons for List -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10" HorizontalAlignment="Right">
                <Button Command="{Binding LoadLeadsCommand}" Style="{StaticResource KlikButtonStyle}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <icon:PackIconFontAwesome Kind="RotateSolid" Height="12" Width="12" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBlock Text="Refresh Leads" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button Command="{Binding ImportLeadsCommand}" Style="{StaticResource KlikButtonStyle}" Visibility="Visible">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <icon:PackIconFontAwesome Kind="ArrowUpFromBracketSolid" Height="12" Width="12" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBlock Text="Import" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button Command="{Binding NewLeadCommand}" Style="{StaticResource KlikButtonStyle}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <icon:PackIconFontAwesome Kind="PlusSolid" Height="12" Width="12" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBlock Text="New Lead" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </StackPanel>
            <!-- Data Grid to display Leads -->
            <DataGrid ItemsSource="{Binding Leads}" 
                  SelectedItem="{Binding SelectedLead, Mode=TwoWay}"
                      AlternatingRowBackground="Cornsilk"
                  AutoGenerateColumns="False"
                      CanUserAddRows="False"
                  Height="450"
                  SelectionMode="Single"
                      ScrollViewer.CanContentScroll="True" 
      ScrollViewer.VerticalScrollBarVisibility="Auto"
      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <DataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}">
                        <Style.Triggers>
                            <!-- Assuming your data item has a boolean property named IsDeleted -->
                            <DataTrigger Binding="{Binding LeadType}" Value="{x:Static helper:LeadType.Dead}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding LeadType}" Value="{x:Static helper:LeadType.New}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding LeadType}" Value="{x:Static helper:LeadType.FollowUp}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>                    
                    <DataGridTemplateColumn Header="Name" Width="*" IsReadOnly="True" HeaderStyle="{StaticResource DataGridHeaderStyle}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Margin="10">
                                    <TextBlock Text="{Binding Company, StringFormat=({0})}" FontSize="10" Margin="0,5,0,10" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                    <Button Content="Update Info" Width="80" Command="{Binding DataContext.UpdateInfoCommand, RelativeSource={RelativeSource AncestorType=local:LeadsView}}" Height="25" HorizontalAlignment="Center" Background="ForestGreen" FontWeight="Bold" Margin="0,10"/>
                                    <Button Content="Edit Info" Width="60" Height="25" Command="{Binding DataContext.UpdateLeadCommand, RelativeSource={RelativeSource AncestorType=local:LeadsView}}" HorizontalAlignment="Center" Background="CornflowerBlue" FontWeight="Bold"/>
                                    <TextBlock HorizontalAlignment="Center" Text="{Binding Id, StringFormat=ID: {0}}" FontWeight="Bold" Margin="0,20"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Contact" Width="*" IsReadOnly="True" HeaderStyle="{StaticResource DataGridHeaderStyle}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="5"/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>                                    
                                    <icon:PackIconFontAwesome Kind="PhoneSolid" Grid.Row="1" Grid.Column="0" FontSize="14" FontWeight="Bold"/>
                                    <icon:PackIconMaterial Kind="Email" Grid.Row="2" Grid.Column="0" FontSize="14" FontWeight="Bold"/>
                                    <icon:PackIconMaterial Kind="MapMarker" Grid.Row="3" Grid.Column="0" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Phone}" Grid.Row="1" Grid.Column="2" FontSize="14"/>
                                    <TextBlock Text="{Binding Email}" Grid.Row="2" Grid.Column="2" FontSize="14"/>
                                    <StackPanel Orientation="Vertical" Grid.Row="3" Grid.Column="2">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding City, StringFormat='{}{0}\, '}" FontSize="12" Margin="0" />
                                            <TextBlock Text="{Binding District, StringFormat='{}{0}\, '}" FontSize="12" Margin="0"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding State, StringFormat='{}{0}\, '}" FontSize="12" Margin="0"/>
                                            <TextBlock Text="{Binding Pincode, StringFormat='{}{0}\, '}" FontSize="12" Margin="0"/>
                                        </StackPanel>                                        
                                        <TextBlock Text="{Binding Country}" FontSize="12" Margin="0"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Lead Info" Width="*" IsReadOnly="True" HeaderStyle="{StaticResource DataGridHeaderStyle}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="LeadSource: " Grid.Row="0" Grid.Column="0" FontSize="14" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Tag: " Grid.Row="1" Grid.Column="0" FontSize="14" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Label: " Grid.Row="2" Grid.Column="0" FontSize="14" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Status: " Grid.Row="3" Grid.Column="0" FontSize="14" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding LeadSource.Name}" TextWrapping="Wrap" Grid.Row="0" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Tags.Name}" TextWrapping="Wrap" Grid.Row="1" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Label.Name}" TextWrapping="Wrap" Grid.Row="2" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Status.Name}" TextWrapping="Wrap" Grid.Row="3" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0" VerticalAlignment="Center"/>
                                </Grid>
                                
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="FollowUp" Width="*" IsReadOnly="True" HeaderStyle="{StaticResource DataGridHeaderStyle}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Last Message: " Grid.Row="0" Grid.Column="0" FontSize="14" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Status: " Grid.Row="1" Grid.Column="0" FontSize="14" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Next FollowUp: " Grid.Row="2" Grid.Column="0" FontSize="14" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding LeadFollowUpModel.LastMsg}" TextWrapping="Wrap" Grid.Row="0" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding LeadFollowUpModel.Name, StringFormat=({0})}" TextWrapping="Wrap" Grid.Row="1" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0,10" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding LeadFollowUpModel.NextFollowUp, StringFormat=F}" TextWrapping="Wrap" Grid.Row="2" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0" VerticalAlignment="Center"/>                                    
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Extra Info" Width="*" IsReadOnly="True" HeaderStyle="{StaticResource DataGridHeaderStyle}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Creation Date: " Grid.Row="0" Grid.Column="0" FontSize="14" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Last Updated: " Grid.Row="1" Grid.Column="0" FontSize="14" Margin="0" VerticalAlignment="Center"/>

                                    <TextBlock Text="{Binding CreationDate, StringFormat=F}" TextWrapping="Wrap" Grid.Row="0" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding UpdationDate, StringFormat=F}" TextWrapping="Wrap" Grid.Row="1" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0,10" VerticalAlignment="Center"/>
                                    
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <!--<DataGridTextColumn Header="Lead Source" Binding="{Binding LeadSource.Name}" Width="*" IsReadOnly="True"/>-->
                    <!--<DataGridTextColumn Header="Tags" Binding="{Binding Tags.Name}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Label" Binding="{Binding Label.Name}" Width="*" IsReadOnly="True"/>-->
                    <!--<DataGridTextColumn Header="Company" Binding="{Binding Company}" Width="*" IsReadOnly="True"/>-->
                    <!--<DataGridTextColumn Header="Status" Binding="{Binding Status.Name}" Width="*" IsReadOnly="True"/>-->
                    <!--<DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="1.5*" IsReadOnly="True"/>-->
                    <!--<DataGridTextColumn Header="Important Date" Binding="{Binding ImpDate}" Width="*" IsReadOnly="True"/>-->

                    <DataGridTemplateColumn Header="Actions" Width="100" HeaderStyle="{StaticResource DataGridHeaderStyle}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Background="Transparent" Command="{Binding DataContext.UpdateLeadCommand, RelativeSource={RelativeSource AncestorType=local:LeadsView}}" BorderThickness="0" HorizontalAlignment="Center" VerticalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                        <icon:FontAwesome Kind="PencilSolid"/>
                                    </Button>
                                    <Button Background="Transparent" Command="{Binding DataContext.DeleteLeadCommand, RelativeSource={RelativeSource AncestorType=local:LeadsView}}" BorderThickness="0" HorizontalAlignment="Center" VerticalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                        <icon:FontAwesome Kind="TrashSolid"/>
                                    </Button>
                                </StackPanel>                                
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <!-- Right Column: Lead Details / CRUD Form -->

    </Grid>
</UserControl>
