<UserControl x:Class="BahiKitab.Views.SettingsCommonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BahiKitab.Views"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:helper="clr-namespace:BahiKitab.Helper"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" x:Name="cvControl">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".4*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding ElementName=cvControl, Path=ControlTitle}" FontSize="14" FontWeight="Bold" FontFamily="Segoe UI" Grid.Row="0" Grid.ColumnSpan="2"/>
        <Grid Grid.Row="2" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Text="Enter Names" FontSize="12" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center"/>
            <TextBox AcceptsReturn="True" Width="180" Grid.Row="1" TextWrapping="Wrap" MaxHeight="100" VerticalScrollBarVisibility="Auto" Margin="10"/>
            <Button Content="Add" Width="60" Padding="0,10" Grid.Row="2" Command="{Binding DynamicAddViewCommand}" CommandParameter="{Binding ElementName=cvControl, Path=DynamicView}"/>
        </Grid>
        <DataGrid ItemsSource="{Binding Leads}" Grid.Row="2" Grid.Column="1" Margin="10"
            SelectedItem="{Binding SelectedLead, Mode=TwoWay}"
            AutoGenerateColumns="False"
            Height="450"
            SelectionMode="Single"
            ScrollViewer.CanContentScroll="True" 
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Actions" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Background="Transparent" Command="{Binding DataContext.DeleteLeadCommand, RelativeSource={RelativeSource AncestorType=local:LeadsView}}" BorderThickness="0" HorizontalAlignment="Center" VerticalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                <icon:FontAwesome Kind="TrashSolid"/>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
