<UserControl x:Class="BahiKitab.Views.SettingsCommonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BahiKitab.Views"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:helper="clr-namespace:BahiKitab.Helper"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" x:Name="cvControl">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".4*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding ElementName=cvControl, Path=ControlTitle}" HorizontalAlignment="Center" FontSize="16" Margin="0,15,0,0" FontWeight="Bold" FontFamily="Segoe UI" Grid.Row="0" Grid.Column="0"/>
        <Grid Grid.Row="2" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Text="Enter Names" FontSize="12" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center"/>
            <TextBox Text="{Binding TextBoxValue, Mode=TwoWay}" AcceptsReturn="True" Width="180" Grid.Row="1" TextWrapping="Wrap" MaxHeight="100" VerticalScrollBarVisibility="Auto" Margin="10"/>
            <Button Content="Add" Width="80" Height="30" FontWeight="Bold" FontSize="14" Grid.Row="2" Command="{Binding DynamicAddViewCommand}" CommandParameter="{Binding ElementName=cvControl, Path=DynamicView}"/>
            <TextBlock Text="*Add Comma Seperated Value for Multiple Addition" Grid.Row="3" FontSize="8" FontStyle="Italic" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </Grid>

        <Button Content="Refresh" FontSize="14" Command="{Binding RefreshCommand}" Height="25" Width="60" FontWeight="Bold" CommandParameter="{Binding ElementName=cvControl, Path=DynamicView}" HorizontalAlignment="Right" Grid.Column="1" VerticalAlignment="Center" Margin="10,0"/>

        <DataGrid ItemsSource="{Binding Data}" Grid.Row="2" Grid.Column="1" Margin="10"
            SelectedItem="{Binding SelectedData, Mode=TwoWay}"
            AutoGenerateColumns="False"
                  CanUserAddRows="False"
            Height="450"
            SelectionMode="Single"
            ScrollViewer.CanContentScroll="True" 
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Actions" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <!--<StackPanel Orientation="Horizontal">
                                <Button Background="Transparent" Margin="2" Command="{Binding DataContext.DeleteLeadCommand, RelativeSource={RelativeSource AncestorType=local:LeadsView}}" BorderThickness="0" HorizontalAlignment="Center" VerticalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                    <icon:FontAwesome Kind="PencilSolid"/>
                                </Button>-->
                            <Button Background="Transparent" Margin="2" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=local:SettingsCommonView}}" CommandParameter="{Binding ElementName=cvControl, Path=DynamicView}" BorderThickness="0" HorizontalAlignment="Center" VerticalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                    <icon:FontAwesome Kind="TrashSolid"/>
                                </Button>
                            <!--</StackPanel>-->                            
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
