<UserControl x:Class="BahiKitab.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BahiKitab.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40">
            <TextBlock Text="Company Profile Settings" FontSize="26" FontWeight="Bold" Margin="0,0,0,25" Foreground="#1A73E8"/>

            <Border Background="White" CornerRadius="10" Padding="25" Margin="0,0,0,20" BorderThickness="1" BorderBrush="#DDD">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Top">
                        <Border Width="120" Height="120" BorderThickness="2" BorderBrush="#E0E0E0" CornerRadius="8" Background="#FAFAFA">
                            <Grid>
                                <TextBlock Text="No Logo" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#AAA"/>
                                <Image Source="{Binding CompanyProfile.LogoPath}" Stretch="Uniform" Margin="5"/>
                            </Grid>
                        </Border>
                        <Button Content="Upload Logo" Command="{Binding UploadLogoCommand}" Margin="0,10,0,0" Cursor="Hand"/>
                    </StackPanel>

                    <Grid Grid.Column="1" Margin="20,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,15">
                            <TextBlock Text="Company Legal Name" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CompanyProfile.CompanyName, UpdateSourceTrigger=PropertyChanged}" Padding="8" FontSize="14"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,15">
                            <TextBlock Text="Proprietor Name" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CompanyProfile.Proprietor}" Padding="8"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,0,15">
                            <TextBlock Text="GST Number" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CompanyProfile.GstNumber}" Padding="8"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Contact Number" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CompanyProfile.Contact}" Padding="8"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Official Email" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CompanyProfile.Email}" Padding="8"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="20" Margin="0,0,10,20" BorderThickness="1" BorderBrush="#DDD">
                    <StackPanel>
                        <TextBlock Text="Bank Details" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                        <TextBlock Text="Bank Name" Margin="0,5,0,2"/>
                        <TextBox Text="{Binding CompanyProfile.BankName}" Padding="5" Margin="0,0,0,10"/>
                        <TextBlock Text="Account Number" Margin="0,5,0,2"/>
                        <TextBox Text="{Binding CompanyProfile.AccountNumber}" Padding="5" Margin="0,0,0,10"/>
                        <TextBlock Text="IFSC Code" Margin="0,5,0,2"/>
                        <TextBox Text="{Binding CompanyProfile.IfscCode}" Padding="5" Margin="0,0,0,10"/>
                        <TextBlock Text="UPI ID" Margin="0,5,0,2"/>
                        <TextBox Text="{Binding CompanyProfile.UpiID}" Padding="5"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="20" Margin="10,0,0,20" BorderThickness="1" BorderBrush="#DDD">
                    <StackPanel>
                        <TextBlock Text="Registered Address" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                        <TextBox Text="{Binding CompanyProfile.Address}" Padding="8" Height="130" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top"/>
                        <TextBlock Text="Company Initials (Max 4 chars)" FontSize="16" FontWeight="Bold" Margin="0,10,0,5"/>
                        <TextBox Text="{Binding CompanyProfile.Initials}" Padding="8"/>
                        <TextBlock Text="Invoice Starting No" FontSize="16" FontWeight="Bold" Margin="0,10,0,5"/>
                        <TextBox Text="{Binding CompanyProfile.InvNo}" Padding="8"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20" BorderThickness="1" BorderBrush="#DDD">
                <StackPanel>
                    <TextBlock Text="Terms &amp; Conditions (To be printed on Invoices)" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBox Text="{Binding CompanyProfile.TermsAndConditions}" Padding="8" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>
                </StackPanel>
            </Border>

            <Button Content="Save Company Profile" Command="{Binding SaveCommand}" 
                    Height="45" Style="{StaticResource KlikButtonStyle}" Foreground="White" FontSize="16" FontWeight="Bold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </ScrollViewer>
</UserControl>
