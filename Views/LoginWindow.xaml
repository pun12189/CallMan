<Window x:Class="BahiKitab.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BahiKitab.Views"
        mc:Ignorable="d"
        Title="Secure Login" Height="450" Width="800" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseDown="Window_MouseDown">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    <Border CornerRadius="10" Background="White" BorderThickness="1" BorderBrush="#DDD">
        <Grid Margin="30">
            <StackPanel Visibility="{Binding IsLoginVisible, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="SIGN IN" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                <TextBlock Text="Username" Margin="0,10,0,5"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="35" FontSize="14" VerticalContentAlignment="Center"/>

                <TextBlock Text="Password" Margin="0,10,0,5"/>
                <PasswordBox x:Name="TxtPassword" Height="35" FontSize="14" VerticalContentAlignment="Center"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" FontSize="11" Margin="0,5"/>

                <Button Command="{Binding LoginCommand}" IsDefault="True"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Height="40" Margin="0,20,0,10" Background="#2193b0" Foreground="White">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="LOGIN" Margin="0,0,10,0"/>
                        <Ellipse x:Name="LoadingSpinner" StrokeThickness="3" Stroke="White" StrokeDashArray="1" Height="20" Width="20"
              Fill="Transparent" RenderTransformOrigin="0.5,0.5"
              Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Ellipse.RenderTransform>
                                <RotateTransform x:Name="noStopRotation" Angle="0" />
                            </Ellipse.RenderTransform>
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="noStopRotation"
                                             Storyboard.TargetProperty="Angle"
                                             From="0" To="360" Duration="0:0:3"
                                             RepeatBehavior="Forever" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>
                    </StackPanel>
                </Button>

                <Grid>
                    <Button Content="Forgot Password?" Command="{Binding SwitchViewCommand}" 
                        Background="Transparent" BorderThickness="0" Foreground="Gray" HorizontalAlignment="Left"/>
                    <Button Content="Exit" Command="{Binding ExitCommand}" 
                        Background="Transparent" BorderThickness="0" Foreground="Gray" HorizontalAlignment="Right"/>
                </Grid>
            </StackPanel>
            <StackPanel Visibility="{Binding IsForgotVisible, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Reset Password" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
                
                
                <TextBlock Text="Enter your registered email to receive a temporary password." TextWrapping="Wrap" Margin="0,0,0,10"/>
                <TextBox Text="{Binding ResetEmail, UpdateSourceTrigger=PropertyChanged}" Tag="Email Address" Height="35" FontSize="14" VerticalContentAlignment="Center" Margin="0,10"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" FontSize="11" Margin="0,5"/>
                <!--<Button Content="Send Reset Link" Command="{Binding SendResetCommand}" Height="40" Margin="0,20,0,10" Background="#2193b0" Foreground="White" />-->
                <Button Command="{Binding SendResetCommand}" IsDefault="True"
      CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
      Height="40" Margin="0,20,0,10" Background="#2193b0" Foreground="White">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Send Reset Link" Margin="0,0,10,0"/>
                        <Ellipse x:Name="LoadingSpinner1" StrokeThickness="3" Stroke="White" StrokeDashArray="1" Height="20" Width="20"
Fill="Transparent" RenderTransformOrigin="0.5,0.5"
Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Ellipse.RenderTransform>
                                <RotateTransform x:Name="noStopRotation1" Angle="0" />
                            </Ellipse.RenderTransform>
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="noStopRotation1"
                               Storyboard.TargetProperty="Angle"
                               From="0" To="360" Duration="0:0:3"
                               RepeatBehavior="Forever" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>
                    </StackPanel>
                </Button>
                <Button Content="Back to Login" 
                Command="{Binding SwitchViewCommand}" 
                Background="Transparent" BorderThickness="0" Margin="0,10" Height="40" Foreground="Gray"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
