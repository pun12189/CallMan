<UserControl x:Class="BahiKitab.Views.StaffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BahiKitab.Views"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="GridActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="8,4">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="User Management" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="Manage system access and user profiles" FontSize="14" Foreground="#7F8C8D" Margin="0,5,0,0"/>
            </StackPanel>

            <Button Content="+ Add New User" 
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Foreground="White"
                    Command="{Binding NewLeadCommand}"
                    Padding="15,8" FontWeight="SemiBold"
                    Style="{StaticResource KlikButtonStyle}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="#D0D0D0" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>

            <DataGrid x:Name="UsersGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#EEEEEE"
                      Background="White"
                      BorderThickness="0"
                      ItemsSource="{Binding Staff}"
                      SelectedItem="{Binding SelectedLead}"
                      VerticalScrollBarVisibility="Auto">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Foreground" Value="#7F8C8D"/>
                        <Setter Property="Padding" Value="15,12"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#EEEEEE"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Foreground" Value="#333"/>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Ellipse Width="40" Height="40">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="{Binding ProfileImage}" Stretch="UniformToFill"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="NAME" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding FullName}" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{Binding Username, StringFormat=({0})}" FontSize="12" Foreground="#999"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="CONTACT" Width="*" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="5"/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <icon:PackIconFontAwesome Kind="PhoneSolid" Grid.Row="1" Grid.Column="0" Margin="0,5" FontSize="14" FontWeight="Bold"/>
                                    <icon:PackIconMaterial Kind="Email" Grid.Row="2" Grid.Column="0" Margin="0,0,0,5" FontSize="14" FontWeight="Bold"/>
                                    <icon:PackIconMaterial Kind="MapMarker" Grid.Row="3" Grid.Column="0" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Phone}" Grid.Row="1" Grid.Column="2" Margin="0,5" FontSize="14"/>
                                    <TextBlock Text="{Binding Email}" Grid.Row="2" Grid.Column="2" Margin="0,0,0,5" FontSize="14"/>
                                    <TextBlock Text="{Binding Address}" Grid.Row="3" Grid.Column="2" FontSize="14"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="INFO" Width="*" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="5"/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>                                        
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <icon:PackIconFontAwesome Kind="BuildingSolid" Grid.Row="0" Grid.Column="0" Margin="0,5" FontSize="14" FontWeight="Bold"/>
                                    <icon:PackIconMaterial Kind="Group" Grid.Row="1" Grid.Column="0" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Department.Name}" Grid.Row="0" Grid.Column="2" FontSize="14" Margin="0,5"/>
                                    <TextBlock Text="{Binding TeamLead.FullName}" Grid.Row="1" Grid.Column="2" FontSize="14"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Extra Info" Width="*" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Creation Date: " Grid.Row="0" Grid.Column="0" FontSize="14" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Last Updated: " Grid.Row="1" Grid.Column="0" FontSize="14" Margin="0" VerticalAlignment="Center"/>

                                    <TextBlock Text="{Binding CreateTime, StringFormat=F}" TextWrapping="Wrap" Grid.Row="0" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding UpdateTime, StringFormat=F}" TextWrapping="Wrap" Grid.Row="1" Grid.Column="1" FontSize="14" FontWeight="Bold" Margin="0,10" VerticalAlignment="Center"/>

                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="ACTIONS" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">

                                    <Button Style="{StaticResource GridActionButton}" Tag="{Binding}" Command="{Binding DataContext.UpdateLeadCommand, RelativeSource={RelativeSource AncestorType=local:StaffView}}">
                                        <TextBlock Text="Edit" Foreground="#2196F3"/>
                                    </Button>

                                    <Button Style="{StaticResource GridActionButton}" Tag="{Binding}" Command="{Binding DataContext.DeleteLeadCommand, RelativeSource={RelativeSource AncestorType=local:StaffView}}">
                                        <TextBlock Text="Delete" Foreground="#E74C3C"/>
                                    </Button>

                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
